{
  "name": "projet-memory-web",
  "version": "1.0.0",
  "description": "Application web Projet Memory",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "cd ../Projet_memory_api && php -S localhost:8000 -t public",
    "dev:full": "concurrently -n \"VITE,API\" -c \"blue,yellow\" \"npm run dev\" \"npm run api\""
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "concurrently": "^8.2.0"
  }
}
